# the weight of a non-negative integer is the number of bits set to 1 in its binary representation
# write a program that takes as input a non-negative integer and returns a number y
# which is not equal to x, but has the same weight and their difference is as small as possible
def same_weight(x: int) -> int:
    return 0